# 💱 Конвертер валют (ЦБ РФ) — Streamlit

<div align="center">

![Python](https://img.shields.io/badge/Python-3.10+-3776AB?style=for-the-badge&logo=python&logoColor=white)
![Streamlit](https://img.shields.io/badge/Streamlit-FF4B4B?style=for-the-badge&logo=streamlit&logoColor=white)
![Pandas](https://img.shields.io/badge/Pandas-150458?style=for-the-badge&logo=pandas&logoColor=white)
![License](https://img.shields.io/badge/License-MIT-green.svg)
![Status](https://img.shields.io/badge/Status-Active-success.svg)

**Простое веб‑приложение для конвертации валют с использованием актуальных курсов Центрального Банка РФ**

[🐛 Report Bug](https://github.com/YOUR_USERNAME/REPO_NAME/issues) · [💡 Request Feature](https://github.com/YOUR_USERNAME/REPO_NAME/issues)

</div>

---

## 📋 Содержание

- [✨ Возможности](#-возможности)
- [🏗️ Архитектура](#️-архитектура)
- [🚀 Быстрый старт](#-быстрый-старт)
- [📦 Установка](#-установка)
- [💻 Использование](#-использование)
- [🛠️ Технологии](#️-технологии)
- [📝 Структура проекта](#-структура-проекта)
- [🎯 Особенности реализации](#-особенности-реализации)
- [⌨️ Полезные команды](#️-полезные-команды)
- [🤝 Вклад в проект](#-вклад-в-проект)

---

## ✨ Возможности

- ✅ **Актуальные курсы** — автоматическая загрузка курсов с официального сайта ЦБ РФ (`https://cbr.ru/currency_base/daily/`)
- ✅ **Умная конвертация** — учёт номинала валют (единиц) для точных расчётов
- ✅ **Удобный интерфейс** — простой и интуитивный веб‑интерфейс на Streamlit
- ✅ **Обработка ошибок** — корректная обработка сетевых и пользовательских ошибок
- ✅ **Кэширование** — оптимизация запросов через кэширование данных (TTL 1 час)
- ✅ **Принудительное обновление** — возможность обновить курсы вручную по кнопке

---

## 🏗️ Архитектура

Проект состоит из трёх модулей:

| Модуль | Описание |
|--------|----------|
| **`data_fetcher.py`** | Загрузка HTML страницы ЦБ и парсинг таблицы курсов в `pandas.DataFrame`, нормализация данных, добавление RUB как базовой валюты |
| **`converter.py`** | Логика конвертации валют с учётом номинала: `amount * (Value_A/Nominal_A) / (Value_B/Nominal_B)` |
| **`app.py`** | Streamlit интерфейс: ввод суммы, выбор валют, отображение результата, кнопка обновления курсов |

### 📊 Схема работы

```
┌─────────────┐
│  ЦБ РФ сайт │
└──────┬──────┘
       │ HTTP GET
       ▼
┌─────────────────┐
│  data_fetcher   │ ──► DataFrame (курсы)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│    converter    │ ──► Конвертация
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│      app.py     │ ──► Веб-интерфейс
└─────────────────┘
```

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)

### Установка за 3 шага

```bash
# 1. Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/REPO_NAME.git
cd REPO_NAME

# 2. Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows PowerShell

# 3. Установить зависимости и запустить
pip install -r requirements.txt
streamlit run app.py
```

🌐 Приложение откроется на `http://localhost:8501`

---

## 📦 Установка

### Подробная инструкция

<details>
<summary>📖 Развернуть инструкцию</summary>

#### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/REPO_NAME.git
cd REPO_NAME
```

#### Шаг 2: Создание виртуального окружения

```bash
# Linux/macOS
python -m venv .venv
source .venv/bin/activate

# Windows PowerShell
python -m venv .venv
.venv\Scripts\activate

# Windows CMD
python -m venv .venv
.venv\Scripts\activate.bat
```

#### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Шаг 4: Запуск приложения

```bash
streamlit run app.py
```

</details>

---

## 💻 Использование

1. **Введите сумму** для конвертации в поле ввода
2. **Выберите валюты**: исходную (From) и целевую (To)
3. **Нажмите "Конвертировать"** — результат отобразится ниже
4. **При необходимости** нажмите "Обновить курсы" для загрузки свежих данных

### 💡 Пример использования

```
Исходная валюта: USD
Целевая валюта: RUB
Сумма: 100

Результат: 100.00 USD = 7500.00 RUB
```

---

## 🛠️ Технологии

### Основные библиотеки

| Библиотека | Минимальная версия | Назначение |
|------------|-------------------|------------|
| `streamlit` | 1.33+ | Веб-интерфейс приложения |
| `pandas` | 2.0+ | Работа с таблицами данных |
| `requests` | 2.31+ | HTTP запросы к сайту ЦБ |
| `beautifulsoup4` | 4.12+ | Парсинг HTML (опционально) |
| `lxml` | 4.9+ | Парсер для HTML таблиц |

### Используемые технологии

<div align="center">

![Python](https://img.shields.io/badge/Python-3.10+-3776AB?style=flat-square&logo=python&logoColor=white)
![Streamlit](https://img.shields.io/badge/Streamlit-FF4B4B?style=flat-square&logo=streamlit&logoColor=white)
![Pandas](https://img.shields.io/badge/Pandas-150458?style=flat-square&logo=pandas&logoColor=white)
![Requests](https://img.shields.io/badge/Requests-000000?style=flat-square&logo=requests&logoColor=white)

</div>

---

## 📝 Структура проекта

```
TechProg/
│
├── 📄 app.py                 # Главный файл Streamlit приложения
├── 📄 converter.py           # Модуль конвертации валют
├── 📄 data_fetcher.py        # Модуль загрузки и парсинга данных
├── 📄 requirements.txt       # Зависимости проекта
├── 📄 README.md              # Документация проекта
└── 📄 .gitignore             # Игнорируемые файлы Git
```

---

## 🎯 Особенности реализации

### Учёт номинала валют

ЦБ РФ публикует курсы за фиксированное количество единиц валюты. Например:
- **USD**: 1 единица = 75 RUB
- **HUF** (венгерский форинт): 10 единиц = 250 RUB

Формула конвертации учитывает это:
```python
rate_per_unit = Value / Nominal  # Цена одной единицы в RUB
result = amount * rate_from / rate_to
```

### Кэширование данных

Используется декоратор `@st.cache_data(ttl=3600)` для:
- ⚡ Снижения нагрузки на сайт ЦБ
- 🚀 Ускорения работы приложения
- 💾 Экономии трафика

### Обработка ошибок

Приложение обрабатывает три типа ошибок:
- **DataFetchError** — проблемы с сетью или недоступность сайта ЦБ
- **DataParseError** — ошибки парсинга HTML (изменилась структура страницы)
- **ConversionError** — ошибки валидации входных данных

---

## ⌨️ Полезные команды

### Команды для работы с проектом

#### Управление виртуальным окружением

```bash
# Создать виртуальное окружение
python -m venv .venv

# Активировать окружение
source .venv/bin/activate        # Linux/macOS
.venv\Scripts\activate           # Windows PowerShell
.venv\Scripts\activate.bat       # Windows CMD

# Деактивировать окружение
deactivate
```

#### Управление зависимостями

```bash
# Установить все зависимости из файла
pip install -r requirements.txt

# Установить конкретный пакет
pip install package_name

# Обновить пакет до последней версии
pip install --upgrade package_name
pip install -U package_name      # короткая форма

# Посмотреть список установленных пакетов
pip list

# Сохранить текущие зависимости в requirements.txt
pip freeze > requirements.txt

# Удалить пакет
pip uninstall package_name

# Показать информацию о пакете
pip show package_name
```

#### Запуск приложения

```bash
# Стандартный запуск
streamlit run app.py

# Запуск на конкретном порту
streamlit run app.py --server.port 8502

# Запуск без автоматического открытия браузера
streamlit run app.py --server.headless true

# Запуск с тёмной темой
streamlit run app.py --theme.base dark
```

#### Python команды

```bash
# Запустить модуль как скрипт
python -m module_name

# Выполнить код из строки
python -c "print('Hello World')"

# Показать версию Python
python --version

# Проверить синтаксис файла без выполнения
python -m py_compile app.py
```

### Команды Git

#### Основные операции

```bash
# Инициализировать репозиторий
git init

# Проверить статус файлов
git status

# Добавить файлы в staging area
git add .                        # добавить все изменения
git add file.py                  # добавить конкретный файл
git add -A                       # добавить все (включая удалённые)
git add -u                       # обновить только отслеживаемые файлы

# Создать коммит
git commit -m "Описание изменений"
git commit -am "Сообщение"       # добавить и закоммитить отслеживаемые файлы

# Просмотр истории
git log                          # полная история
git log --oneline                # краткая история
git log --graph --oneline --all  # график веток

# Отменить изменения
git checkout -- file.py          # отменить изменения в файле
git reset HEAD file.py           # убрать файл из staging area
git reset --soft HEAD~1         # отменить последний коммит, сохранив изменения
```

#### Работа с ветками

```bash
# Создать новую ветку
git branch branch_name
git checkout -b branch_name      # создать и переключиться

# Переключиться на ветку
git checkout branch_name
git switch branch_name           # современная команда

# Посмотреть все ветки
git branch                       # локальные
git branch -a                    # все (включая удалённые)

# Удалить ветку
git branch -d branch_name        # безопасное удаление
git branch -D branch_name         # принудительное удаление

# Переименовать текущую ветку
git branch -m new_name
```

#### Работа с удалённым репозиторием

```bash
# Добавить удалённый репозиторий
git remote add origin https://github.com/USERNAME/REPO.git

# Посмотреть удалённые репозитории
git remote -v

# Загрузить изменения с GitHub
git pull origin main
git fetch origin                 # только скачать, не объединять

# Отправить изменения на GitHub
git push origin main
git push -u origin main          # установить upstream ветку

# Клонировать репозиторий
git clone https://github.com/USERNAME/REPO.git
git clone -b branch_name REPO.git # клонировать конкретную ветку
```

#### Полезные Git команды

```bash
# Показать различия
git diff                         # изменения в рабочей директории
git diff --staged                # изменения в staging area
git diff HEAD                    # все изменения с последнего коммита

# Просмотр конкретного коммита
git show commit_hash

# Переместить незакоммиченные изменения в stash
git stash                        # сохранить изменения
git stash list                   # список stash
git stash pop                    # применить и удалить последний stash
git stash apply                  # применить stash, но не удалять

# Создать тег
git tag v1.0.0                   # лёгкий тег
git tag -a v1.0.0 -m "Версия 1.0.0"  # аннотированный тег
git push origin v1.0.0           # отправить тег на GitHub

# Игнорирование файлов
# Файлы перечисляются в .gitignore
# После изменения .gitignore:
git rm --cached file.py          # удалить из индекса, но оставить файл
```

### Объяснение популярных флагов

#### Флаги pip
- `-r` — читать зависимости из файла (requirements.txt)
- `-U` или `--upgrade` — обновить пакет до последней версии
- `-e` — установить пакет в режиме разработки (editable)

#### Флаги Python
- `-m` — запустить модуль как скрипт
- `-c` — выполнить код из строки
- `-i` — интерактивный режим после выполнения скрипта
- `-u` — небуферизованный вывод
- `-v` — подробный вывод (verbose)

#### Флаги Git
- `-m` — сообщение коммита
- `-a` — добавить все отслеживаемые файлы
- `-A` — добавить все файлы (включая удалённые)
- `-u` — установить upstream ветку
- `-b` — создать ветку
- `-d` — безопасное удаление
- `-D` — принудительное удаление
- `-v` — подробный вывод (verbose)
- `--oneline` — краткий формат вывода
- `--graph` — показать график веток

#### Флаги Streamlit
- `--server.port` — указать порт для запуска
- `--server.headless` — запуск без браузера
- `--theme.base` — выбрать тему (light/dark)

### Полезные команды для отладки

```bash
# Проверить подключение к сайту ЦБ
curl https://cbr.ru/currency_base/daily/

# Проверить установленные Python пакеты
pip list | grep streamlit

# Посмотреть размер проекта
du -sh .                         # Linux/macOS
Get-ChildItem -Recurse | Measure-Object -Property Length -Sum  # Windows PowerShell

# Найти все Python файлы
find . -name "*.py"             # Linux/macOS
Get-ChildItem -Recurse -Filter *.py  # Windows PowerShell

# Проверить синтаксис всех Python файлов
python -m py_compile *.py
```

---

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! Вот как вы можете помочь:

1. 🍴 **Форкните** репозиторий
2. 🌿 **Создайте** ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. 💾 **Закоммитьте** изменения (`git commit -m 'Add some AmazingFeature'`)
4. 📤 **Запушьте** в ветку (`git push origin feature/AmazingFeature`)
5. 🔀 **Откройте** Pull Request

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

---

## 👨‍💻 Автор

**Ваше Имя**

- GitHub: [@YOUR_USERNAME](https://github.com/YOUR_USERNAME)
- Email: your.email@example.com

---

## 🙏 Благодарности

- Центральному Банку РФ за предоставление актуальных курсов валют
- Команде Streamlit за отличный фреймворк
- Всем контрибьюторам проекта

---

## 📚 Дополнительная информация

### Примечания

- Данные курсов загружаются из официальной таблицы ЦБ РФ. Столбец «Курс» публикуется за фиксированное число единиц валюты («Единиц»), поэтому при конвертации учитывается номинал: `Value/Nominal` — цена одной единицы в RUB.
- При недоступности источника или изменении структуры страницы вы увидите сообщение об ошибке в интерфейсе.
- Кэш автоматически обновляется раз в час, но вы можете принудительно обновить данные через кнопку "Обновить курсы".

---

<div align="center">

⭐️ Если проект был полезен, поставьте звезду! ⭐️

[⬆ Наверх](#-конвертер-валют-цб-рф--streamlit)

</div>
